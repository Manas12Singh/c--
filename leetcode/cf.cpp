#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, k, x;
        cin >> n >> k >> x;
        if (k == 1)
            cout << "NO" << endl;
        else
        {
            if(k==2 && x==1 && )
        }
    }
    return 0;
}