#include <iostream>

using namespace std;

int main()
{
    float a, b, avg;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    avg = (a + b) / 2;
    cout << "Average is: " << avg << endl;
    return 0;
}
