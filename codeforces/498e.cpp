#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> nums(7);
    for (int &i : nums)
        cin >> i;
    int l = 0;
    for (int i = 0; i < 7; i++)
    {
        if (nums[i] == 0)
            continue;
        
    }
}